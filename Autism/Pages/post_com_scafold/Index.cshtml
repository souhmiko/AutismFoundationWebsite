@page
@model Autism.Pages.post_com_scafold.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PostComment[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostComment[0].Published)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostComment[0].CreatedAtdate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostComment[0].PublishedAtdate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostComment[0].Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostComment[0].Parent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostComment[0].Post)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PostComment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Published)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAtdate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PublishedAtdate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Parent.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Post.Title)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
